<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.philippe.seg3125test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Learn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProfileActivity"
            android:label="Profile" />
        <activity
            android:name=".GlossaryActivity"
            android:label="Glossary" />
        <activity
            android:name=".MathTopicsActivity"
            android:label="Mathematics" />
        <activity
            android:name=".Trigonometry"
            android:label="Trigonometry" />
        <activity android:name=".AlgebraActivity" />
        <activity android:name=".AlgebraQuizActivity"></activity>
        <activity android:name=".AlgebraQuiz2Activity"></activity>
    </application>

</manifest>
